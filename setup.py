from setuptools import find_packages, setup
setup(
    name='hses_fred',
    packages=find_packages(),
    version='1.0',
    description='FReD - Firewall Ruleset Distributor: A Tool to distribute central firewall rules to decentralized filter points in a network',
    author='Samuel Mueller',
)